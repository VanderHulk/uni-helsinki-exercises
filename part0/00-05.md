```mermaid

sequenceDiagram
    participant browser
    participant server

    browser ->> server: GET https://studies.cs.helsinki.fi/exampleapp/spa
    activate server
    server -->> browser: text/html
    deactivate server
    
    Note right of browser: discovers href, makes a request
    browser ->> server: GET https://studies.cs.helsinki.fi/exampleapp/main.css
    activate server
    server -->> browser: text/css
    deactivate server

    Note right of browser: discovers src, makes a request
    browser ->> server: GET https://studies.cs.helsinki.fi/exampleapp/spa.js
    activate server
    server -->> browser: application/javascript
    deactivate server

    Note right of browser: starts executing spa.js, discovers XMLHttpRequest (makes a request to the server), runs callback function when data.json is ready
    browser ->> server: GET /exampleapp/data.json
    activate server
    server -->> browser: application/json
    deactivate server
    Note right of browser: parses JSON, redraws notes in the page