```mermaid

sequenceDiagram
    participant browser
    participant server

browser ->> server: POST https://studies.cs.helsinki.fi/exampleapp/new_note_spa (json data content and timestamp)
Note right of browser: clicking Save, triggers onsubmit function to send POST request to the server
server -->> browser: 201 created application/json
Note left of server: server responds
Note right of browser: parses JSON, redraws notes in the page